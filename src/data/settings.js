const radiobase = "http://localhost:3000/"

export default {
    "radiobase": {
        "timetable": radiobase + "timetable/",
        "stations": radiobase + "stations/v2",
        "audio": radiobase + "audio/",
        "auto": {
            "reserve": radiobase + "auto/reserve/"
        }
    }
}